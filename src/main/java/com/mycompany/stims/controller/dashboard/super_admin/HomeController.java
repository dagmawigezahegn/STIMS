package com.mycompany.stims.controller.dashboard.super_admin;

import java.net.URL;
import java.util.ResourceBundle;
import javafx.fxml.Initializable;

/**
 * The HomeController class is responsible for managing the super admin home
 * screen. It initializes and controls the UI components displayed on the home
 * screen. This class implements the {@link Initializable} interface to handle
 * initialization logic after the FXML file has been loaded.
 */
public class HomeController implements Initializable {

    /**
     * Initializes the controller after its root element has been completely
     * processed. This method is automatically called by JavaFX after the FXML
     * file has been loaded.
     *
     * @param url The location used to resolve relative paths for the root
     * object, or null if the location is not known.
     * @param rb The resources used to localize the root object, or null if the
     * root object was not localized.
     */
    @Override
    public void initialize(URL url, ResourceBundle rb) {
        // TODO: Add initialization logic here
    }
}
